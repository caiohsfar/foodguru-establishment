import React, { Component } from 'react';
import { Text, View } from 'react-native';

export default class Products extends Component {
  render() {
    return (
      <View>
        <Text>
          {' '}
          SESSÕES DE PRATOS AQUI (DISCUTIR COMO SERÁ FEITO)/ BARRA DE BUSCA / FAB / MULTI-SELECT
          {' '}
        </Text>
      </View>
    );
  }
}
